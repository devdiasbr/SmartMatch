{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/[[...route]]" }
  ],
  "functions": {
    "api/[[...route]].ts": {
      "maxDuration": 60
    }
  }
}
